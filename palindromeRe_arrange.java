
public class palindromeRe_arrange {
	public boolean palindromeRearranging(String inputString) {
	    //get the letters. count the letters. If even then ok. if odd count the odd. if only one then ok.
	    int letterCount = inputString.length()+1;//starting from 1
	    if(letterCount % 2 != 0){
	        
	    }
	}
}
